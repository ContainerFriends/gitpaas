/**
 * Service model
 */
export interface Service {
    id: string;
    name: string;
    repositoryUrl: string;
    branch: string;
    projectId: string;
    createdAt: string;
    updatedAt: string;
}